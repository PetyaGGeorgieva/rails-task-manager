<h2><%= @task.title %></h2>

<p><%= @task.details %></p>

<div>
Completed:
<%= check_box_tag 'task[completed]', @task.completed, @task.completed, class: 'form-check-input' %>
</div>
<%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>

<%= link_to 'Back to', index_path %>
